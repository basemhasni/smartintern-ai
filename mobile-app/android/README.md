# Android runner

Flutter SDK is not available in the current execution environment, so the Android runner was not generated by `flutter create`.

When Flutter is installed, run from `mobile-app/`:

```bash
flutter create . --platforms=android,ios
```

Then keep the existing `lib/` foundation and generated platform runners.

