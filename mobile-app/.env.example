# Optional override. Omit it for Expo Web and iOS Simulator on localhost.
# Android Emulator uses http://10.0.2.2:5000/api automatically.
# A real phone must use the computer's LAN address.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
