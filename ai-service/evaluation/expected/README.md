# Expected Results

The expected ranges in `evaluation/cases/*.json` are calibration targets, not
absolute truth. They document how the deterministic AI services should behave
on controlled scenarios.

Update expected values only when the underlying AI logic is intentionally
changed and the new behavior is manually reviewed.
